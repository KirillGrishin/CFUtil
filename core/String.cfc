component {

	/* This is intended to be used by other functions in this module that have predefined format */
	public string function format(required string str, string mask)
	description = "Formats a string according to the mask, which must be a valid regex"
	{

	}

}